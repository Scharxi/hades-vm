fun swapAndAdd(a: Int, b: Int): Int {
    return b + a
}

fun duplicateAndMul(x: Int): Int {
    return x * x
}

fun pickThird(a: Int, b: Int, c: Int): Int {
    return a
}

fun main(): Int {
    val x = swapAndAdd(5, 3)
    val y = duplicateAndMul(x)
    return pickThird(y, 100, 200)
} 