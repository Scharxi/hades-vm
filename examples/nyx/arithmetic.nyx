fn add(a: i32, b: i32) -> i32 {
    a + b
}

fn sub(a: i32, b: i32) -> i32 {
    a - b
}

fn mul(a: i32, b: i32) -> i32 {
    a * b
}

fn div(a: i32, b: i32) -> i32 {
    a / b
}

fn main() -> i32 {
    let x = add(10, 5);
    let y = sub(x, 3);
    let z = mul(y, 2);
    div(z, 3)
} 